[pytest]
# The backend imports as if `src` were the root - `from db import ...`, not
# `from src.db import ...` - because that is how it is started, both by
# scripts/dev.py and inside the PyInstaller bundle. The tests import it the same
# way rather than making the package layout mean something different under test.
pythonpath = src
testpaths = tests
