# Build-time only dependencies, not needed to run anydeck.
# Installed automatically by scripts/build.py.

-r requirements.txt

# Bundles backend + frontend into a standalone application.
pyinstaller==6.22.0

# Tests. Run from this directory:
#   .venv/bin/python -m pytest
pytest==9.1.1
