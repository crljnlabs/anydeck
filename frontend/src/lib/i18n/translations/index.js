import de from './de'
import en from './en'

/** One file per language. Adding a language is a file here and an entry in
 *  `languages.js` - nothing else in the app has to know. */
export const strings = { de, en }
